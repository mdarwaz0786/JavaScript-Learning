// find():
// Returns the first element in the array that satisfies a given testing function, or undefined if none is found.

const numbers = [1, 2, 3, 4, 5];
const evenNumber = numbers.find((num) => num % 2 === 0);
console.log(evenNumber); // Output: 2

// Finished.
