// push():
// Adds one or more elements to the end of the array and returns the new length of the array.

const fruits = ['apple', 'orange', 'banana', 'grape'];
fruits.push('pear');
console.log(fruits); // Output: ['apple', 'orange', 'banana', 'grape', 'pear']

// Finished.