// charAt(index):
// The charAt(index) method returns the character at a specified index in a string. 
// The index starts from 0.

const str = "Hello World";
console.log(str.charAt(0)); // Output: H
console.log(str.charAt(6)); // Output: W

// Finished.