// split(separator, limit): 
// The split() method split a string into an array of substring based on the specified separator. 
// You can also provide an optional limit to limit the number of split.

const fruits = "apple orange banana";
const splitFruits = fruits.split(" ");
console.log(splitFruits);       // Output: ["apple", "orange", "banana"]

// Finished.

