// substr(start, length):
// Extracts a substring starting from the 'start' index with a specified 'length'.

const str = "Hello, World!";
console.log(str.substr(7, 5)); // Output: "World"
console.log(str.substr(-6, 3)); // Output: "Wor" (start from the 6th character from the end)

// Finished.