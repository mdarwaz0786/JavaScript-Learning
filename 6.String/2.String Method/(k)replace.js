// replace(searchValue, newValue):
// This method replace the first occurrence of the searchValue with the newValue in the string.

const string = "Hello, World, World";
const newString = string.replace("World", "Universe");
console.log(newString);   // Output: "Hello, Universe, World"


const str = "A R W A Z"
console.log(str.replace(" ", "")); // output: "AR W A Z"
console.log(str);

// Finished.