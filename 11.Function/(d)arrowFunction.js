// Arrow Function:
// Arrow functions provide a more concise syntax for writing functions, especially for single-expression functions.

// create a simple arrow function that adds two numbers:
const addNumbers = (a, b) => {
    return a + b;
};
const result = addNumbers(5, 10);
console.log(result); // Output: 15

// Finished.