// Type Conversion (coercion):
// JavaScript allow implicit and explicit type conversion between different data type.

// implicit Conversion:
// Implicit convert number to string.
let number = 42;
console.log(typeof number); // output: number
let string = String(number);
console.log(typeof string); // output: string

// Explicit Conversion:
// Explicit convert the string to number.
let strNum = "42";
console.log(typeof strNum); // output: string
let num = Number(strNum);
console.log(typeof num); // output: number

// Finished.